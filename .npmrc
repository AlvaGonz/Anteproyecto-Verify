# Enforce pnpm as the only allowed package manager
# Running `npm install` or `yarn install` will fail with a clear error
only-allow=pnpm

# pnpm best practices
shamefully-hoist=false
strict-peer-dependencies=false
engine-strict=true
