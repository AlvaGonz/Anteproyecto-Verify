{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0",
    "npm": "FORBIDDEN — use pnpm"
  },
  "scripts": {
    "preinstall": "npx --yes only-allow pnpm",
    "dev": "pnpm --filter web-frontend dev",
    "build": "pnpm --filter web-frontend build",
    "preview": "pnpm --filter web-frontend preview",
    "clean": "pnpm --filter web-frontend clean",
    "lint": "pnpm --filter web-frontend lint",
    "db:seed": "docker compose exec -T api dotnet run --project /src/src/backend/Tools/DbSeeder/DbSeeder.csproj"
  },
  "dependencies": {},
  "devDependencies": {}
}
